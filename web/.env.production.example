# Copy to .env.production for a manual production build, or set GitHub secret VITE_API_URL
# GoDaddy VPS API (after deploy) — must include /api suffix
VITE_API_URL=https://api.yourdomain.com/api

# GitHub Pages subpath (match your repo name)
VITE_BASE_PATH=/AI-TimeTable-Assistant/
